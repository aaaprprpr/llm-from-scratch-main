# 2026-09-23 远端预训练 40k 步复核

## 结论

**退化在 40k 步没有恢复，也没有继续恶化，而是稳定停在一个远差于起点的平台上。** 40k 的 val loss=4.9097，低于 20k 的 5.0500，但仍比 5k 的 3.7172 差了 1.19 nats。本次最佳 checkpoint 是 5k。

**"这是数据集问题"这个判断，现有证据不支持它作为退化的主因。** 数据集确实有问题，但它的作用是压低质量上限，不是让训练崩掉。判定依据是两组对照，见下文第 3、4 节：退化的幅度与窗口语言无关，而权重的增长与优化器分组严格对应。两者都不是数据配比能产生的现象。

本次检查全程只读：复制了 checkpoint、日志和 CSV 到本机评估，没有中断远端进程（PID 373949 仍在运行，100% GPU、287W），没有修改远端任何代码、配置、数据或 checkpoint。

## 检查对象与方法

- 机器：`tdt@192.168.1.3`，RTX 5070 Ti 16GB；训练进程 PID 373949，检查时约 43,140 步。
- 项目：`/media/tdt/新加卷/llm-from-scratch-main`，run `output/train_logs/run_20260922_103413`。
- 本机 RTX 5060 Ti，用与上一轮完全相同的固定 train/val 窗口（32 窗口 × 2048 tokens）复算，未争用远端 GPU。
- 复算校验：本机 15k/25k/35k val loss 为 4.9283 / 4.9947 / 4.9625，远端 `metrics.csv` 为 4.9279 / 4.9950 / 4.9622，差 <0.001。评估管线可信。
- 新增复制 15k、25k、35k、40k checkpoint 到 `output/remote_audit_20260923/checkpoints/`。

## 1. 固定集合上的 loss

| checkpoint | 本机 train loss | 本机 val loss | 相对 5k 的 val 变化 |
| ---: | ---: | ---: | ---: |
| 5,000 | 3.8810 | **3.7172** | — |
| 10,000 | 4.3908 | 4.2502 | +0.5330 |
| 15,000 | 5.0519 | 4.9283 | +1.2111 |
| 20,000 | 5.1571 | 5.0505 | +1.3333 |
| 25,000 | 5.1137 | 4.9947 | +1.2775 |
| 30,000 | 5.0478 | 4.9465 | +1.2293 |
| 35,000 | 5.0662 | 4.9625 | +1.2453 |
| 40,000 | 4.9792 | 4.9097 | +1.1925 |

日志中最低 val loss 是 4,500 步的 3.5336，但 checkpoint 每 5,000 步存一次，**4.5k 那个最优点从未落盘，已经不可恢复**。现存最好的资产就是 5k。

15k 之后 val 一直在 4.91～5.05 区间震荡，没有再恶化，也没有回到 4.5 以内。40k 相对 30k 只进步了 0.037，属于噪声量级。继续同配置跑下去没有依据。

## 2. 生成质量：40k 明显差于 5k

同一组提示、temperature=0.3、固定种子：

```text
5k    输入：因为外面下着很大的雨，所以我
      续写：,那就是"我们现在做到的,现在我们搞了,现在我们搞了……   （复读，但语法通）

40k   输入：因为外面下着很大的雨，所以我
      续写：,那就是在外面,它可以避免在在的,因为了.如果没有一个例子,
            那么就会不能把我的身上……                        （语法破碎）

5k    输入：小明把红色钥匙放进左边抽屉……红色钥匙原来在
      续写：红河中,马上返回直边,然后又返回住红河中,红河边区边区边区……（复读但有实体）

40k   输入：小明把红色钥匙放进左边抽屉……红色钥匙原来在
      续写：红花花花,把红花花,把红花,把红花红花,把红花红花红花木红花……（字符级崩塌）

40k   输入：你好，我是
      续写：!<|endoftext|>                                    （第二个 token 就吐 EOS）
```

40k 出现 5k 没有的失效模式：直接复述提示词、字符级循环、以及问候语触发 EOS。5k 的复读是"黏在模板上"，40k 是"结构本身散架"。

## 3. 决定性对照一：退化与语言无关

把 32 个固定验证窗口按中文占比（汉字/字母字符）排序，比较 5k 与 40k 的逐窗口 loss：

| 分组 | 窗口数 | 5k loss | 40k loss | 平均变化 | 变化倍数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 低中文 <50% | 7 | 2.1507 | 2.8544 | +0.7037 | **×1.327** |
| 混合 50–80% | 1 | 3.5031 | 4.4870 | +0.9839 | ×1.281 |
| 中文为主 ≥80% | 24 | 4.1830 | 5.5267 | +1.3437 | **×1.321** |

**32 / 32 个窗口全部恶化**，包括 7 个纯英文窗口。三个语言分组的变化倍数几乎相同（1.28～1.33）。

这一条直接否证了"英文语料把模型带坏"的机制：如果退化源于语言配比，纯英文窗口应当最稳、中文窗口被拉低；实际是各语言等比下滑。等比、全局的下滑是模型自身崩坏的形状，不是数据构成问题的形状。

（注意分组间绝对变化不可直接比：英文文本熵低，5k 时 loss 本来就只有 2.15，+0.70 已是 33% 的相对涨幅。）中文占比与退化幅度的 Pearson r=+0.806，方向是"中文越多反而退化越多"，与数据污染假说相反。

## 4. 决定性对照二：权重增长严格跟随优化器分组

同一次训练、同一批数据，唯一差别是参数落在哪个优化器组：

| 参数组 | 优化器 | weight_decay | 5k→40k 权重 RMS 变化 |
| --- | --- | ---: | ---: |
| attn out_proj | Muon | **0.0** | **×3.952** |
| attn qkv_proj | Muon | **0.0** | **×3.063** |
| ffn w2 | Muon | **0.0** | **×2.939** |
| ffn w1 / w3 | Muon | **0.0** | **×2.886** |
| RMSNorm gain | AdamW | 0.0（norm 不衰减） | ×1.027 |
| embedding | AdamW | 0.1 | **×0.690** |

**唯一带 weight decay 的参数组在收缩，不带衰减的 Muon 组（47,775,744 参数，占模型 77%）全部涨 3～4 倍。** 数据对所有参数组是同一份，只有分组不同，而增长只发生在无衰减组。这是本次 run 内部自带的对照实验。

配置确认：`configs/pretrain.json` 中 `optimizer.weight_decay=0.1` 只作用于 AdamW 组；`optimizer.muon.weight_decay=0.0`，而 Muon 组持有 77% 的参数（`pretrain/run_train_model.py:157`）。

## 5. 隐藏状态与梯度同步失控

| step | qkv 权重 RMS | ffn.w2 RMS | 末层残差 RMS | val loss | grad_norm 中位 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 5,000 | 0.2580 | 0.1604 | 946 | 3.7172 | 0.91 |
| 10,000 | 0.3868 | 0.2353 | 3,799 | 4.2502 | 38.13 |
| 15,000 | 0.4829 | 0.2940 | 11,950 | 4.9283 | 104.32 |
| 20,000 | 0.5620 | 0.3409 | 22,504 | 5.0505 | 128.35 |
| 25,000 | 0.6303 | 0.3810 | 43,701 | 4.9947 | 119.49 |
| 30,000 | 0.6903 | 0.4155 | 73,509 | 4.9465 | 125.04 |
| 35,000 | 0.7440 | 0.4465 | 99,538 | 4.9625 | 122.63 |
| 40,000 | 0.7917 | 0.4745 | **120,735** | 4.9097 | 130.31 |

残差流从 946 涨到 120,735（**128 倍**），全部权重仍是有限值，没有 NaN，`norm` 层把输出重新归一化到 RMS 3.16，所以生成不会数值溢出——但"没有 NaN"不等于训练健康。

梯度范数（裁剪前）分段统计，本次扩展到 43k：

| 训练区间 | 中位数 | 95% 分位 | 最大 |
| --- | ---: | ---: | ---: |
| 1k～3k | 0.246 | 0.432 | 0.775 |
| 3k～5k | 0.480 | 0.950 | 1.159 |
| 5k～10k | 9.055 | 45.482 | 422 |
| 10k～15k | 74.178 | 328.655 | 2,672 |
| 15k～20k | 119.932 | 402.861 | 2,737 |
| 20k～25k | 124.567 | 523.795 | 6,080 |
| 25k～30k | 124.281 | 496.683 | 7,627 |
| 30k～35k | 122.591 | 775.323 | **19,272** |
| 35k～40k | 127.831 | 611.436 | 9,329 |
| 40k～43k | 124.338 | 819.730 | 18,494 |

中位数稳定在 120～130，没有回落迹象；尾部继续出现 1～2 万量级的尖峰。裁剪到 1.0 能限制单步步长，但挡不住持续的方向性漂移。

## 6. 数据集问题：真实存在，但不是本次退化的原因

数据集的问题上一轮已经查实，本次没有推翻：

- 11 个来源中只启用了 `fineweb_edu_zh_4_5` 一个（`configs/data_pipeline.json:299-345`）。
- 4,096 篇完整文档抽样中，中文比例 <20% 的占 15.1% 文档、**26.28% tokens**，全部来自 `IndustryCorpus2`（该来源占 38.22% tokens，其中 57% 文档是低中文）。
- 存在超长英文医学/生物论文（最长 63,921 tokens），携带引用、表格和 LaTeX。
- 现有清洗 17,790,513 条只过滤 748 条（0.0042%），未覆盖语言、篇内重复和网页模板。

**这些都会压低一个 62M 中文模型的质量上限，应当修。但它们解释不了本次 run 里 val loss 在固定集合上从 3.53 涨到 4.91。** 数据构成问题是给模型设一个更高的地板，不会让已经在 4.5k 达到 3.53 的模型把它丢掉。丢掉的机制是第 4、5 节的无衰减权重漂移。

修数据能让下一个 run 的上限更高；不修优化器，下一个 run 大概率还是会在 5k 附近塌掉。

## 7. 建议

1. **停掉当前 run。** 40k 相对 5k 没有任何一处更好，已连续 28k 步停在劣质平台，继续跑到 228k 只是消耗 GPU。保留 5k 和 30k/40k 文件做对照。本次没有替你停，进程仍在跑。
2. **先做优化器对照，再谈重训。** 上一轮报告（`docs/pretraining-run-20260922-audit.md`）建议的全 AdamW 基线至今没有跑。同结构、同词表、同数据顺序、同验证窗口，LR=3e-4、betas=(0.9,0.95)、矩阵 weight_decay=0.1、norm 不衰减、梯度裁剪=1。观察点必须越过 4.5k～5k 这个退化起点，不能再看单步成功。
3. **若继续用 Muon，必须给 Muon 组加非零 weight decay**（例如 0.1），并把 `muon.max_lr` 从 0.02 降到 0.003～0.005。这两个是待验证的起点，不是已证明的结论；一次只改一个变量。QK normalization 是候选但属架构改动，要单独验证。
4. **恢复 5k 时要小心优化器状态。** `load_state_dict` 会带回旧优化器的 param groups，只改 JSON 里的 `weight_decay` 再普通 resume 会被 checkpoint 里的 0.0 覆盖，需要显式覆盖恢复后的参数组，或只加载模型权重重建优化器。
5. **稳定性确认后再重建数据。** 按上一轮 `docs/fineweb-edu-zh-cleaning-audit.md` 的建议做中文语言过滤（主池 ≥70%）、篇内重复清理和来源 token 配额，重点削 `IndustryCorpus2` 的长英文文献。写入新路径 `train_zh_v2.bin`，不要在训练中替换同名文件。词表保持 24,576。
6. **短期提 checkpoint 频率。** 最优点落在 4.5k 而 checkpoint 在 5k 间隔上，好模型直接丢了。既然退化发生在 5k 附近，间隔应临时调到 500～1000 步。

## 8. 证据与复现

目录：`output/remote_audit_20260923/`。

- `evaluate_40k.py`：本轮评估脚本，复用上一轮的固定窗口，可指定步数。
- `evaluation_metrics_40k.json`：5k/15k/25k/35k/40k 的固定 train/val loss、参数统计、激活统计。
- `evaluation_40k.json`：5k 与 40k 的完整生成结果与汇总。
- `language_vs_degradation.py` / `language_vs_degradation.json`：32 个验证窗口的语言比例、逐窗口 loss 与相关性。
- `train_metrics_40k.csv`、`samples_40k.csv`：远端训练指标与训练中采样的快照。
- `checkpoints/`：本机新增的 15k/25k/35k/40k 副本。
- 上一轮证据仍在 `output/remote_audit_20260922/`。
